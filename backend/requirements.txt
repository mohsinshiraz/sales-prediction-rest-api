# Dependencies installed inside the backend container.
# The scikit-learn, NumPy, pandas, xgboost and joblib pins MUST match the
# versions used to train and serialize the model - a mismatch can make
# joblib.load() warn or fail when the container starts.
pandas==2.2.2
numpy==2.0.2
scikit-learn==1.6.1
xgboost==2.1.4
joblib==1.4.2
Werkzeug==2.2.2
flask==2.2.2
gunicorn==20.1.0
requests==2.28.1
uvicorn[standard]
streamlit==1.43.2
